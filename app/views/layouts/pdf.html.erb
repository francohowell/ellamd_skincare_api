<%
##
# A layout for HTML-based PDFs generated by the app.
#
# This layout includes all of the necessary CSS embedded within it to generate a good-looking PDF.
# The CSS is used in the Prescription PDF template.
%>
<!doctype html>
<html>
  <head>
    <meta charset="utf-8" />
    <style>
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      body,
      html {
        margin: 0;
        padding: 0;
        font-family: "helvetica";
        font-size: 12px;
        color: #333333;
      }

      .content {
        padding: 25px;
      }

      h1 {
        font-weight: 400;
        font-size: 26px;
        margin-bottom: 20px;
      }

      h1 strong {
        font-weight: 700;
      }

      h2 {
        font-size: 16px;
        padding-bottom: 5px;
        margin-bottom: 0;
        border-bottom: 1px solid #DDDDDD;
        color: #777777;
      }

      table {
        border-collapse: collapse;
        border-spacing: 0;
        width: 100%;
        margin-bottom: 20px;
      }

      p {
        margin-top: 8px;
        margin-bottom: 30px;
      }

      td {
        padding: 7px 15px 7px 0;
        border-bottom: 1px solid #EEEEEE;
        vertical-align: top;
      }

      tr:last-child td {
        border-bottom: 0;
      }

      .field-name {
        width: 140px;
        font-weight: 700;
      }

      .columns {
        overflow: hidden;
        margin-left: -30px;
      }

      .columns::after {
        content: "";
        display: block;
        clear: left;
      }

      .column {
        width: 50%;
        padding-left: 30px;
        float: left;
      }

      .signature {
        width: 200px;
      }

      .token {
        float: right;
        font-size: 20px;
        color: #AAAAAA;
      }
    </style>
  </head>

  <body>
    <div class="content">
      <%= yield -%>
    </div>
  </body>
</html>
